import React from "react";

const EditBlogModal = ({ params }: { params: { id: string } }) => {
  console.log(2);
  console.log(params.id);
  return <div>EditBlogModal</div>;
};

export default EditBlogModal;
